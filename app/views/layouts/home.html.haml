!!!
%html
  %head
    %title //OGApg//
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    - if Ogapg::Application.assets.find_asset("#{controller_name}/#{action_name}.js")
      = javascript_include_tag "#{controller_name}/#{action_name}"
    = csrf_meta_tags
  %body
    .wrapper
      = render 'snippets/navbar'

      .container-fluid.squeeze
        .row-fluid
          .span3.center
            %h3.underlined
              = link_to 'Productos', products_path, class: 'act'
              %i.icon-arrow-right
            %h3.underlined= link_to 'Política de envíos', 'shipping_policy', class: 'act'
            %h3= link_to 'Acerca de las promociones', 'about_promos', class: 'act'
          .span9= render 'snippets/carousel'
      
      .container-fluid.squeeze
        = render 'snippets/alerts' 
        = yield

      .chunk
      .push

    .footer
      .pull-right
        = render 'snippets/session_links'